# CropSense Environment Variables
# Replace placeholder values with real ones as we build each phase

# Backend API URL (local development — we'll replace with Render URL later)
CROPSENSE_API_URL=http://localhost:8000

# Grok API Key (get free at console.x.ai — we'll use this in Phase 8)
GROK_API_KEY=your_grok_api_key_here

# App version
APP_VERSION=1.0.0